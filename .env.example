# Environment Configuration Template
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Environment: development, testing, production
ENVIRONMENT=development

# Enable debug mode (never use in production)
DEBUG=true

# API Server
HOST=0.0.0.0
PORT=8000
RELOAD=true

# Logging
LOG_LEVEL=INFO

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:19006

# =============================================================================
# SPEECH PROVIDER CONFIGURATION
# =============================================================================

# Provider: mock, azure, whisper, riva
SPEECH_PROVIDER=mock

# Azure Speech Service (if using Azure)
AZURE_SPEECH_KEY=your-azure-subscription-key-here
AZURE_SPEECH_REGION=eastus
AZURE_SPEECH_LANGUAGE=ar-SA

# =============================================================================
# STORAGE CONFIGURATION
# =============================================================================

# Storage Type: memory, redis, supabase
# WARNING: Use 'memory' only for development - data is lost on restart
STORAGE_TYPE=memory

# Redis (recommended for production)
REDIS_URL=redis://localhost:6379

# Supabase (for full-featured storage with SQL)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key

# =============================================================================
# READING TUTOR SETTINGS
# =============================================================================

# Confidence threshold for hesitation detection (0.0-1.0)
CONFIDENCE_THRESHOLD=0.7

# Word similarity threshold for matching (0.0-1.0)
SIMILARITY_THRESHOLD=0.8

# Enable strict mode by default (enforce Arabic diacritics)
STRICT_MODE_DEFAULT=false

# Session settings
SESSION_TTL=3600  # Session expiry in seconds (1 hour)
MAX_SESSION_SIZE=1000  # Maximum sentences per session

# =============================================================================
# PRODUCTION DEPLOYMENT (Railway/Render/Heroku)
# =============================================================================

# These are set automatically by hosting platforms:
# - PORT (Railway, Heroku)
# - REDIS_URL (if Redis addon is enabled)

# For Railway deployment:
# 1. Connect your GitHub repo
# 2. Add environment variables in Railway dashboard
# 3. Set ENVIRONMENT=production
# 4. Deploy!

# =============================================================================
# DEVELOPMENT SHORTCUTS
# =============================================================================

# Quick configs for common scenarios:

# Local Development (in-memory):
# ENVIRONMENT=development
# STORAGE_TYPE=memory
# SPEECH_PROVIDER=mock

# Local Development (with Redis):
# ENVIRONMENT=development
# STORAGE_TYPE=redis
# REDIS_URL=redis://localhost:6379
# SPEECH_PROVIDER=mock

# Production (Railway with Redis addon):
# ENVIRONMENT=production
# STORAGE_TYPE=redis
# REDIS_URL=${REDIS_URL}  # Provided by Railway
# SPEECH_PROVIDER=azure
# AZURE_SPEECH_KEY=your-key
# AZURE_SPEECH_REGION=eastus
