# Core API Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-multipart==0.0.6

# Optional: Storage Backends
redis[asyncio]==5.0.1  # For Redis storage (production recommended)
# supabase==2.3.0  # Uncomment for Supabase storage

# Optional: Speech Providers
azure-cognitiveservices-speech==1.34.0  # For Azure Speech
# openai-whisper==20231117  # Uncomment for Whisper

# Development Tools
pytest==7.4.4
pytest-asyncio==0.23.3
black==24.1.0
flake8==7.0.0

# Production Server
gunicorn==21.2.0

# Note: Core logic (app/core/text_processor.py) has ZERO external dependencies
# It uses only Python standard library (re, unicodedata, difflib, dataclasses, enum)
